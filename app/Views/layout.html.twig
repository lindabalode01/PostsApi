<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Articles</title>
    <style>
    </style>
</head>
<body>
{% for article in articles %}
    <div class="article">
        <h1>{{ article.title }}</h1>
        <p>{{ article.body }}</p>
        <div class="author-info">
            <p>Author: {{ getUsers(article.userId)[0].username }}</p>
        </div>
        <div class="comments">
            <h2>Comments:</h2>
            {% set articleComments = getComments(article.id) %}
            {% if articleComments %}
                {% for comment in articleComments %}
                    <div class="comment">
                        <p><strong>{{ comment.name }}</strong></p>
                        <p>{{ comment.body }}</p>
                    </div>
                {% endfor %}
            {% else %}
                <p>No comments available.</p>
            {% endif %}
        </div>
    </div>
{% endfor %}
</body>
</html>