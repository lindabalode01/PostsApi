<!DOCTYPE html>
<html lang="">
<head>
    <title>All Users</title>
</head>
<body>
<h1>All Users</h1>
<ul>
    {% for user in users %}
        <li>
            <h2>User ID: {{ user.getId() }}</h2>
            <p>Name: {{ user.getName() }}</p>
            <p>Username: {{ user.getUsername() }}</p>
            <p>Email: {{ user.getEmail() }}</p>
            <p>Phone: {{ user.getPhoneNumber() }}</p>
            <p>Website: {{ user.getWebsite() }}</p>
        </li>
    {% endfor %}
</ul>
</body>
</html>